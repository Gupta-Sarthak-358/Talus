[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "talus-routing"
version = "0.1.0"
dependencies = ["networkx"]

[tool.setuptools]
packages = ["routing"]
