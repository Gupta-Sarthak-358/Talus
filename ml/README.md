# ml

Machine learning components for the Talus risk engine.

- `data_generation/` — physics-informed synthetic data generator (FoS-based labels).
- `training/` — Random Forest training + probability calibration.
- `evaluation/` — metrics, reliability diagrams, feature ablation.
- `models/` — trained artifacts (git-ignored).

See `docs/04_MODEL_PLAN.md`.