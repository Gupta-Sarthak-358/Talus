# Talus Frontend — Live SIH26001 (real NGEN + trained model)
# Live backend is now the default. Mock fallback kept only for offline demo if :8000 is down.
VITE_USE_LIVE_API=true

# Live FastAPI endpoint base URL (scaffold S1-S4 89/78/66/52, 32 zones x 8 corridors, reports, routing, simulation, training 2936 GroupKFold8 RF 0.9345)
VITE_API_URL=http://localhost:8000/api
