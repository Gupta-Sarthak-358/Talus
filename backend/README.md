# backend

FastAPI backend for Talus.

- `app/` — application code (zone, risk, explanation, trend, decision, route, simulation APIs).
- `tests/` — API tests.

See `docs/05_API_SPEC.md` for the frozen API contract.